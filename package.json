{
  "name": "twilight-world",
  "version": "0.1.0",
  "private": true,
  "description": "Twilight World, a fast, ad-free, mobile-first Twilight Imperium 4E reference",
  "type": "module",
  "scripts": {
    "fetch": "node scripts/fetch-source.mjs",
    "extract": "node scripts/extract.mjs && node scripts/extract-cards.mjs && node scripts/extract-map.mjs",
    "build": "node scripts/build.mjs",
    "all": "npm run fetch && npm run extract && npm run build",
    "serve": "node scripts/serve.mjs",
    "test": "python3 -m unittest discover -s tests"
  }
}
