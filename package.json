{
  "name": "ti4info",
  "version": "0.1.0",
  "private": true,
  "description": "Fast, ad-free, mobile-first Twilight Imperium 4E reference",
  "type": "module",
  "scripts": {
    "fetch": "node scripts/fetch-source.mjs",
    "extract": "node scripts/extract.mjs",
    "build": "node scripts/build.mjs",
    "all": "npm run fetch && npm run extract && npm run build",
    "serve": "node scripts/serve.mjs"
  }
}
